notify('Error! Cannot update post. Please check Approve or Reject before pressing Ok', 'Refresh', "#")

<% if @post.errors.any? %>
	<% @post.errors.full_messages.each do |x| %>
		notify("<%= x %>")
	<% end %>
<% end %>
