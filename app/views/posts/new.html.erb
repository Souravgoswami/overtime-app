<h1 class="centrify"><span class="glyphicon40 glyphicon-write">Overtime Request<span></h1>
<br>

<% if @post.errors.any? %>
	<% @post.errors.full_messages.each do |x| %>
		<% notify(x) %>
	<% end %>
<% end %>

<%= render 'form' %>
